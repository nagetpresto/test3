apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80  # Exposes Nginx to external traffic
      targetPort: 80
  type: LoadBalancer  # or NodePort if you don't want to use LoadBalancer
